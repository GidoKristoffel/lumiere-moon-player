import { VisibilityControlDirective } from './visibility-control.directive';

describe('VisibilityControlDirective', () => {
  it('should create an instance', () => {
    const directive = new VisibilityControlDirective();
    expect(directive).toBeTruthy();
  });
});
