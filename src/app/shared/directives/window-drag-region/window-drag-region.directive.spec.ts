import { WindowDragRegionDirective } from './window-drag-region.directive';

describe('WindowDragRegionDirective', () => {
  it('should create an instance', () => {
    const directive = new WindowDragRegionDirective();
    expect(directive).toBeTruthy();
  });
});
