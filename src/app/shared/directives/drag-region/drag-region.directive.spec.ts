import { DragRegionDirective } from './drag-region.directive';

describe('DragRegionDirective', () => {
  it('should create an instance', () => {
    const directive = new DragRegionDirective();
    expect(directive).toBeTruthy();
  });
});
